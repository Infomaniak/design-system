---
'@infomaniak-design-system/tokens': patch
---

Fixed the dark mode `color-bg-feedback-warning` dim2 token, which referenced the wrong color family (red instead of orange). The warning background now renders with the intended orange shade in dark mode.
