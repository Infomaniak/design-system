---
'@infomaniak-design-system/tokens': minor
'@infomaniak-design-system/components': minor
---

Updated the `esds-separator` default design: the line now uses the stronger neutral border color (`color.border.dim2`), and a default 2px padding (`spacing.2xs`) is applied across the line so it no longer touches adjacent content.
