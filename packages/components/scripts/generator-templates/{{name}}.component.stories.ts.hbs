import type { Meta, StoryObj } from '@storybook/web-components-vite';
import { getStorybookHelpers } from '@wc-toolkit/storybook-helpers';
import './{{name}}.component.js';
import { {{pascalCase name}} } from './{{name}}.component.js';

const { args, argTypes, template } = getStorybookHelpers<{{pascalCase name}}>('{{name}}');

const meta = {
  title: 'Components/{{storyTitle name}}',
  component: '{{name}}',
  tags: ['autodocs'],
  args,
  argTypes,
  render: (args) => template(args),
} satisfies Meta<{{pascalCase name}}>;

export default meta;

type Story = StoryObj<{{pascalCase name}}>;

export const Default: Story = {
  args: {
    label: '{{storyTitle name}}',
  },
};
